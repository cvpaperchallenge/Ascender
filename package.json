{
  "name": "ascender",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "node-sass src/sass/main.scss --output-style compressed --output build",
    "watch": "node-sass src/sass/main.scss --output-style compressed --output build --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cvpaperchallenge/Ascender.git"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^7.0.1"
  }
}
